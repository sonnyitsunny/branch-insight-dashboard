# AGENTS.md

이 문서는 Claude Code와 OpenAI Codex가 이 저장소에서 작업할 때 공통으로 따라야 하는
프로젝트 규칙의 단일 원본이다. 도구별 작업 절차는 이 문서에 넣지 않는다.

---

## 1. 프로젝트 목적

Python 기반의 업무용 데이터 대시보드를 개발한다. 산출물은 두 가지다.

- Dash 서버로 실행하는 대화형 대시보드
- 서버 없이 브라우저에서 열 수 있는 정적 HTML 보고서(스냅샷)

초기 개발은 더미 데이터로 진행하고, 이후 실제 데이터를 연결한다.
실행 환경은 인터넷이 차단된 내부망을 전제로 한다.

---

## 2. 공통 기술 스택

- Python 3.12
- Dash
- Plotly
- dash-ag-grid
- pandas
- HTML / CSS
- Python 표준 라이브러리

Node.js와 React를 직접 사용하지 않는다.

다음은 사용자 승인 없이 추가하지 않는다.

- dash-bootstrap-components, Bootstrap, Tailwind CSS, Font Awesome
- 외부 JavaScript 테이블 라이브러리, 외부 CSS 프레임워크
- Google Fonts, 기타 CDN 기반 라이브러리

새로운 Python 패키지가 필요하다고 판단되면 설치하거나 `requirements.txt`에 추가하기 전에
다음을 사용자에게 먼저 설명하고 승인을 받는다.

1. 패키지가 필요한 이유
2. 기존 기술 스택만으로 구현하기 어려운 이유
3. 내부망에서 사용할 수 있는지
4. 정적 HTML 내보내기에 미치는 영향
5. 추가하지 않고 구현할 수 있는 대안

---

## 3. 최우선 제약사항

우선순위가 충돌할 때 아래 항목이 다른 편의성보다 앞선다.

1. 내부망·오프라인 실행 가능성 (외부 네트워크 의존 금지)
2. 데이터 계층과 UI 계층의 분리 (더미 데이터 → 실제 데이터 교체 가능성)
3. Dash 화면과 정적 HTML 보고서의 공통 로직 재사용
4. 디자인 토큰 일원화
5. 최소 범위 수정 (요청 없는 리팩터링 금지)

---

## 4. 문서의 범위

이 문서는 디자인 규칙과 개발 규칙만 정의한다.
대시보드에 담길 업무 내용은 아직 확정되지 않았으므로 다음을 문서에 작성하지 않는다.

- KPI 이름, 카드에 표시할 지표, 탭 이름, 화면 문구, 대시보드 제목
- 차트 제목·개수·종류·축 구성
- 업무 지표 계산식, 전월/전년 대비 기준
- 데이터 컬럼명, 테이블 컬럼명, 필터 항목
- 고객·자산·거래 등 구체적인 업무 영역
- 실제 데이터베이스 종류, 실제 테이블명, 실제 API 주소, 사내 시스템 이름

프로젝트 루트에 `dashboard_sketch.jpeg`가 있으면 다음 용도로만 참고한다.

- 전체 화면 밀도, 상단 요약 영역, 탐색·선택 영역,
  차트 배치 영역, 상세 테이블 영역, 카드와 차트의 시각적 계층,
  데스크톱 대시보드의 대략적인 레이아웃

스케치에 적힌 구체적인 지표명이나 업무 내용은 이 문서에 옮기지 않는다.

---

## 5. 디자인 시스템

디자인 참고 자료: 미래에셋증권 CI 가이드
(https://securities.miraeasset.com/newir/view/pc/kr/ciguide.jsp)

이 URL은 디자인 확인용 참고 자료일 뿐이다.
완성된 애플리케이션이 실행 중에 이 URL에 접근하거나, 이 사이트에서
CSS·폰트·이미지·JavaScript를 불러오면 안 된다.

### 5.1 색상 토큰

주색상

- Mirae Asset Orange: `#F58220`
- Mirae Asset Blue: `#043B72`

그래프 및 시각화 보조색

- `#F0B26B`, `#CB6015`, `#AD624E`, `#84888B`
- `#00A9CE`, `#0086B8`, `#7E9FC3`, `#8DC8E8`

배경 및 구분선 보조색

- `#48535B`, `#C2AC97`, `#84888B`, `#A0A6A8`
- `#CDCECB`, `#E5E4E1`, `#ECEFF4`

### 5.2 색상 사용 원칙

- Orange를 핵심 강조색으로 사용하고, Blue는 Orange를 보조하는 색상으로 사용한다.
- Orange를 화면 전체에 과도하게 사용하지 않는다.
- 일반 배경은 흰색 또는 밝은 회색 계열을 사용한다.
- 카드와 주요 콘텐츠 배경은 흰색을 기본으로 한다.
- 구분선은 밝은 회색 계열을 사용한다.
- 본문 텍스트는 검정에 가까운 회색을 사용한다.
- 상태나 의미를 색상만으로 구분하지 않는다(레이블, 아이콘, 텍스트를 병행).

### 5.3 토큰 관리

- CSS와 Plotly Figure에 임의의 색상 값을 직접 반복해서 작성하지 않는다.
- 공통 디자인 토큰은 CSS 변수와 Python 상수로 각각 한 곳에서 관리한다.
- CSS 변수명과 Python 상수명의 의미를 일관되게 유지한다.
- Plotly 차트도 CSS와 동일한 색상 토큰을 사용한다.

### 5.4 글꼴

기본 글꼴은 Spoqa Han Sans이며, CSS 글꼴 스택은 다음을 기준으로 한다.

```
"Spoqa Han Sans Neo", "Spoqa Han Sans", "Malgun Gothic", sans-serif
```

- 실행 PC에 설치된 Spoqa Han Sans를 우선 사용한다.
- 설치되어 있지 않으면 Malgun Gothic 또는 sans-serif로 대체한다.
- Google Fonts를 사용하지 않고, 외부 URL에서 폰트를 불러오지 않는다.
- 사용자가 제공하지 않은 폰트 파일을 자동으로 다운로드하지 않는다.
- 라이선스가 확인되지 않은 폰트 파일을 저장소에 추가하지 않는다.
- Plotly Figure의 `layout.font.family`에도 동일한 글꼴 스택을 사용한다.
- 글꼴 설정을 여러 위치에 중복하지 않고 공통 디자인 설정에서 한 번만 관리한다.

---

## 6. UI 및 레이아웃 원칙

- 데스크톱 내부 업무 환경을 우선한다.
- 화면이 좁아질 때 자연스럽게 열이 접히도록 반응형 레이아웃을 고려한다.
- 레이아웃은 CSS Grid와 Flexbox로 구성한다.
- 주요 영역의 좌우 정렬과 간격을 일관되게 유지한다.
- 카드, 차트, 필터, 테이블의 시각적 계층을 명확하게 한다.
- 화면이 좁아지면 다열 구성을 한 열로 변경할 수 있게 한다.
- 고정 픽셀 너비를 과도하게 사용하지 않는다.
- 과도한 그림자, 그라데이션, 장식 효과를 사용하지 않는다.
- 모서리 둥글기는 작고 절제되게 사용한다.
- Plotly 기본 배경과 불필요한 도구를 정리한다.
- 차트 여백, 글꼴, 범례, hover 형식을 일관되게 관리한다.
- 숫자 표시 형식은 공통 함수로 관리한다.
- 키보드 포커스 표시를 제거하지 않는다.
- 컴포넌트 ID는 의미 있는 이름을 사용하고 프로젝트 전체에서 규칙을 통일한다.
- 구체적인 대시보드 콘텐츠는 코드 구현 시 별도 요구사항으로 정의한다.

---

## 7. 내부망 및 오프라인 원칙

### 7.1 실행 제약

- 실행 시 외부 인터넷 연결을 요구하지 않는다.
- CDN을 사용하지 않는다.
- 외부 URL의 CSS, JavaScript, 폰트, 이미지, 아이콘을 사용하지 않는다.
- Google Fonts를 사용하지 않는다.
- 원격 API를 기본 데이터 소스로 사용하지 않는다.
- 애플리케이션 실행 중 CI 가이드 사이트에 접근하지 않는다.
- CSS와 이미지는 프로젝트 내부 `assets` 디렉터리에서 관리한다.
- Dash가 프론트엔드 파일을 로컬에서 제공하도록 구성한다.
- dash-ag-grid는 설치된 Python 패키지에 포함된 로컬 리소스를 사용한다.
- 외부 JavaScript 파일을 직접 삽입하지 않는다.
- 실행 시 패키지를 자동으로 다운로드하는 코드를 작성하지 않는다.
- 실제 접속 정보나 인증 정보를 저장소에 넣지 않는다.

### 7.2 패키지 반입

- 패키지 버전은 `requirements.txt`에서 고정할 수 있게 한다.
- 내부망 반입을 위해 Python 패키지와 의존성 목록을 명확히 관리한다.
- 필요하면 인터넷 환경에서 wheel 파일을 미리 준비해 내부망으로 반입할 수 있게 한다.

### 7.3 오프라인 검증 기준

- 네트워크를 차단한 상태에서 Dash 앱 실행
- 브라우저 개발자 도구에서 외부 네트워크 요청 확인
- 소스 코드에서 `http://`, `https://` 참조 검색
- CSS의 `@import` 및 외부 `url()` 참조 확인
- 생성된 HTML의 외부 스크립트·외부 스타일 참조 확인
- 로컬 `assets` 파일 누락 여부 확인
- 정적 HTML을 네트워크 없이 직접 열어 확인

---

## 8. 권장 프로젝트 구조

구조를 처음부터 과도하게 복잡하게 만들지 않는다.

```
project-root/
├── app.py
├── dashboard/
│   ├── __init__.py
│   ├── data.py
│   ├── figures.py
│   ├── grid.py
│   ├── layout.py
│   ├── callbacks.py
│   └── export_html.py
├── assets/
│   └── style.css
├── data/
│   └── sample/
├── tests/
├── requirements.txt
├── AGENTS.md
└── CLAUDE.md
```

역할

- `app.py` — Dash 애플리케이션 생성과 실행 진입점.
  구체적인 레이아웃이나 데이터 처리 로직을 과도하게 넣지 않는다.
- `dashboard/data.py` — 데이터 소스 인터페이스, 더미 데이터 로딩,
  실제 데이터 연결 어댑터, 데이터 검증·정규화, UI에서 사용할 공통 데이터 형태 반환.
- `dashboard/figures.py` — Plotly Figure 생성 함수.
  데이터를 입력받아 Figure를 반환하며, 데이터 조회나 파일 읽기를 직접 수행하지 않는다.
- `dashboard/grid.py` — dash-ag-grid `columnDefs` 생성, `rowData` 변환,
  그리드 표시 형식 관리, 정적 HTML 테이블 변환에 필요한 공통 포맷 관리.
- `dashboard/layout.py` — Dash 레이아웃과 재사용 가능한 UI 컴포넌트.
  데이터 파일이나 데이터베이스를 직접 읽지 않는다.
- `dashboard/callbacks.py` — Dash 콜백 등록.
  데이터 소스의 구체적인 연결 방식을 알지 않도록 구성하고,
  콜백 안에서 파일 경로나 SQL을 직접 작성하지 않는다.
- `dashboard/export_html.py` — 정적 HTML 보고서 생성.
  Dash 서버 없이 열 수 있는 HTML을 만들고, 기존 데이터 처리 함수와
  Figure 생성 함수를 재사용한다.
- `assets/style.css` — 공통 CSS, 디자인 토큰, 반응형 레이아웃.
  외부 CSS·폰트 참조 금지.
- `data/sample/` — 개발용 더미 데이터. 실제 개인정보가 없는 가상 데이터만 저장.
- `tests/` — 데이터 계약, 데이터 정규화, Figure 생성, 그리드 설정,
  HTML 내보내기 검증.

실제로 필요해지기 전에는 다음 디렉터리를 만들지 않는다.
`services`, `repositories`, `helpers`, `utils`, `components`, `adapters`, `config`, `docs`.
프로젝트 규모가 커지고 역할 분리가 명확하게 필요할 때만 확장한다.

---

## 9. 데이터 소스 분리

- 레이아웃, 차트, 그리드, 콜백이 CSV 파일을 직접 읽지 않는다.
- 레이아웃, 차트, 그리드, 콜백이 데이터베이스에 직접 접속하지 않는다.
- 데이터 접근은 `dashboard/data.py`를 통해서만 수행한다.
- 외부 코드에는 일관된 데이터 로딩 함수만 공개한다.
  예: `load_dashboard_data(filters=None)` (형태 예시일 뿐이며,
  구체적인 업무 컬럼이나 반환 데이터는 정의하지 않는다.)
- 더미 데이터와 실제 데이터가 동일한 반환 구조를 사용하도록 한다.
- 실제 데이터 연결 방식이 바뀌어도 UI 코드를 최대한 수정하지 않도록 한다.
- 데이터 소스별 차이는 데이터 계층에서 처리한다.

### 9.1 데이터 소스 선택

개발 단계에서 다음 데이터 소스를 교체할 수 있게 설계한다.

- `sample`
- `local_file`
- `internal_source`

데이터 소스 선택을 코드 여러 곳에 하드코딩하지 않고,
환경 변수 또는 하나의 공통 설정값으로 선택할 수 있게 한다.
예: `DASHBOARD_DATA_SOURCE=sample`

현재 단계에서는 실제 데이터베이스 종류나 연결 방식을 확정하지 않는다.

### 9.2 데이터 정규화

원본 컬럼명이나 데이터 타입이 달라도 UI 계층에는 동일한 형태로 전달되도록 하며,
다음 처리는 데이터 계층이 담당한다.

- 컬럼명 매핑
- 데이터 타입 변환
- 누락값 처리
- 필수 컬럼 확인
- 날짜 형식 정규화
- 숫자 형식 정규화
- 중복 데이터 처리
- 빈 데이터 처리
- 데이터 유효성 검사

구체적인 컬럼명과 업무 규칙은 아직 정의하지 않는다.

---

## 10. 더미 데이터와 실제 데이터 연결

### 10.1 더미 데이터 원칙

- 실제 고객 정보처럼 보이는 개인정보를 사용하지 않는다.
- 실명, 전화번호, 계좌번호, 주소 등을 생성하지 않는다.
- 필요하면 일반적인 가상 식별자만 사용한다.
- 실행할 때마다 무작위로 달라지지 않도록 고정 seed를 고려하고,
  테스트 결과를 재현할 수 있게 한다.
- 더미 데이터 생성 로직과 UI 코드를 분리한다.
- 더미 데이터도 실제 데이터와 동일한 검증 단계를 통과하게 한다.
- 더미 데이터 전용 분기문을 UI 코드 곳곳에 작성하지 않는다.

### 10.2 실제 데이터 연결 원칙

실제 데이터를 연결할 때 다음 부분만 교체하거나 추가할 수 있어야 한다.

- 데이터 읽기 구현
- 원본 컬럼과 내부 표준 컬럼의 매핑
- 인증 및 연결 설정
- 필요한 조회 조건

다음 부분은 최대한 변경하지 않도록 설계한다.

- 레이아웃
- Plotly Figure 생성 함수
- dash-ag-grid 설정 함수
- 공통 숫자 포맷 함수
- HTML 내보내기 로직

실제 접속 정보는 코드에 작성하지 않고, 환경 변수나 내부 보안 설정을 통해 전달한다.
대상: 서버 주소, 포트, 사용자 ID, 비밀번호, 인증 토큰, 데이터베이스명, 내부 API 주소.
현재 단계에서는 실제 접속 방식과 인증 방식을 임의로 결정하지 않는다.

---

## 11. Dash 및 콜백 규칙

- 앱 생성, 레이아웃, 콜백, 데이터 로직을 분리한다.
- 콜백에서 파일을 직접 읽지 않는다.
- 콜백에서 SQL을 직접 작성하지 않는다.
- 콜백에서 데이터 소스 종류를 직접 판별하지 않는다.
- 하나의 콜백에 너무 많은 역할을 넣지 않고, 입력과 출력의 책임을 명확히 한다.
- 전역 가변 데이터를 콜백에서 수정하지 않는다.
- 동일한 데이터 전처리를 여러 콜백에서 반복하지 않는다.
- 초기 로딩이 무거워지지 않도록 고려한다.
- 빈 데이터가 전달되어도 화면이 깨지지 않게 한다.
- 콜백 오류를 무조건 숨기지 않고, 필요한 경우 사용자가 이해할 수 있는
  오류 상태를 표시한다.
- 운영 코드에 `debug=True`를 고정하지 않는다.
- 존재하지 않는 ID를 콜백에서 참조하지 않는다.
- 사용자 요청 없이 `suppress_callback_exceptions`를 무분별하게 사용하지 않는다.

---

## 12. Plotly 규칙

- Figure 생성 함수는 데이터를 입력받아 Figure를 반환하며 데이터를 직접 조회하지 않는다.
- 공통 Plotly 레이아웃 설정을 재사용한다.
- 브랜드 색상과 글꼴을 공통 설정에서 가져오고,
  차트마다 임의의 글꼴·색상을 반복해서 작성하지 않는다.
- hover template 형식을 일관되게 관리한다.
- 불필요한 배경, 격자선, modebar 기능을 정리한다.
- 반응형 크기를 고려한다.
- 데이터가 비어 있을 때 빈 Figure 또는 안내 상태를 반환할 수 있게 한다.
- 정적 HTML 내보내기에서도 같은 Figure 생성 함수를 사용하고,
  Dash 화면용과 HTML용 Figure를 별도로 중복 구현하지 않는다.

구체적인 차트 종류, 제목, 축, 업무 데이터는 정의하지 않는다.

---

## 13. dash-ag-grid 규칙

- Dash 실행 화면의 업무용 테이블에는 `dash_ag_grid.AgGrid`를 사용한다.
- dash-ag-grid를 공식 프로젝트 의존성으로 관리하고,
  Python 3.12와 호환되는 검증된 버전을 `requirements.txt`에 고정한다.
- 외부 CDN이나 원격 JavaScript를 직접 참조하지 않는다.
- `rowData` 생성과 `columnDefs` 생성을 각각 별도 함수로 분리한다.
- 레이아웃 파일에 복잡한 `columnDefs`를 직접 작성하지 않는다.
- 정렬, 필터, 페이지네이션, 컬럼 크기 조절을 지원할 수 있게 하되,
  데이터 규모를 고려해 필요한 기능만 활성화한다.
- 숫자, 날짜, 비율 등의 포맷은 공통 함수로 관리한다.
- 실제 컬럼명이 확정되기 전에는 업무 컬럼을 문서에서 정의하지 않는다.
- 사용자 승인 없이 외부 JavaScript cell renderer를 추가하지 않는다.
- `unsafe_allow_jscode`는 반드시 필요한 경우에만 검토한다.
- 내부망과 보안 요건을 고려해 임의의 JavaScript 실행을 피한다.
- 그리드 데이터 가공 로직을 콜백이나 레이아웃에 중복 작성하지 않는다.

---

## 14. 정적 HTML 내보내기

Dash 애플리케이션과 정적 HTML 보고서는 서로 다른 실행 방식이다.

### 14.1 Dash 실행 모드

Dash 서버를 실행해 브라우저로 접속하는 방식이며 다음 기능을 사용할 수 있다.

- Python 콜백
- 서버 측 데이터 처리
- 동적 화면 갱신
- dash-ag-grid 기능
- 사용자의 선택에 따른 데이터 재조회

### 14.2 정적 HTML 모드

서버 없이 브라우저에서 직접 여는 HTML 보고서이며,
특정 시점의 데이터를 내보내는 스냅샷 방식으로 설계한다.

- Dash 앱 전체를 그대로 HTML 하나로 변환한다고 표현하지 않는다.
- Dash 서버용 화면과 정적 HTML 보고서를 별도 산출물로 관리한다.
- 두 산출물이 동일한 데이터 처리 함수와 동일한 Figure 생성 함수를 사용한다.
- Plotly 차트는 `to_html()` 또는 동등한 기능으로 HTML로 변환한다.
- Plotly.js는 외부 CDN이 아니라 HTML 파일 안에 포함한다.
- 한 HTML 파일에 Plotly.js가 불필요하게 여러 번 포함되지 않게 한다.
- CSS는 HTML 내부에 포함하거나 로컬 파일을 사용한다.
- 외부 JavaScript, 외부 CSS, 외부 폰트를 참조하지 않는다.
- 생성된 HTML을 인터넷 없이 열 수 있어야 한다.

### 14.3 AgGrid의 HTML 변환

Dash 화면의 AgGrid 컴포넌트 자체를 정적 HTML에 그대로 저장하려고 하지 않는다.

- AgGrid에 사용한 것과 동일한 원본 데이터를 사용한다.
- 동일한 데이터 정규화·포맷 함수를 사용한다.
- 정적 HTML에서는 일반 HTML `<table>`로 렌더링한다.
- pandas `DataFrame.to_html()` 또는 별도의 안전한 HTML 테이블 생성 함수를 사용할 수 있다.
- 숫자 정렬, 헤더 스타일, 행 구분선은 CSS로 구현한다.
- HTML 특수문자 이스케이프를 적용하고,
  실제 데이터에 HTML이 포함되어 있어도 임의 실행되지 않게 한다.

### 14.4 유지되는 기능과 유지되지 않는 기능

정적 HTML에서 기본적으로 유지되지 않는 것

- Python 콜백
- 서버 측 데이터 재조회
- 서버 측 필터 계산
- Dash 컴포넌트 상태 변경
- AgGrid의 모든 동적 기능

정적 HTML에서도 유지할 수 있는 Plotly 브라우저 기능

- hover
- 확대 및 축소
- 범례 선택
- 차트 이미지 저장 등 Plotly 기본 상호작용

정적 HTML에서도 테이블의 정렬·필터·페이지네이션이 반드시 필요해지면
별도의 JavaScript 구현 요구사항으로 검토하며,
사용자 승인 없이 외부 테이블 라이브러리를 추가하지 않는다.

---

## 15. 코딩 규칙

- Python 3.12를 기준으로 작성한다.
- 변수명과 함수명은 영어 snake_case, 클래스명은 PascalCase,
  상수는 UPPER_SNAKE_CASE를 사용한다.
- 사용자에게 보이는 문구는 한글을 기본으로 한다.
- 타입 힌트를 가능한 범위에서 사용한다.
- 함수는 한 가지 역할을 갖게 하고, 데이터 처리와 UI 생성을 분리한다.
- 파일 경로를 코드 여러 곳에 하드코딩하지 않는다.
- 데이터 소스 설정을 코드 여러 곳에 중복하지 않는다.
- 숫자와 날짜 포맷을 공통 함수로 관리한다.
- 동일한 디자인 값을 여러 파일에 직접 작성하지 않는다.
- 빈 데이터와 누락값을 처리한다.
- 예외를 무조건 무시하지 않는다.
- 실제 개인정보를 로그에 출력하지 않는다.
- 비밀번호와 접속 정보를 코드에 작성하지 않는다.
- 사용자가 요청하지 않은 대규모 리팩터링을 하지 않고,
  기존 동작을 유지하면서 최소 범위로 수정한다.
- 새로운 패키지를 승인 없이 추가하지 않는다.
- 불필요한 추상화나 클래스 계층을 만들지 않고,
  현재 규모에 필요한 수준으로만 구조를 분리한다.

---

## 16. 테스트 및 검증

코드 수정 후 가능한 범위에서 다음을 수행한다.

- 사용 중인 Python 버전 확인
- Python 3.12 환경에서 import 확인
- `python -m compileall .`
- 관련 테스트 실행
- Dash 앱 시작 확인
- 초기 화면 렌더링 확인
- 콜백 ID 중복 확인
- 존재하지 않는 ID 참조 확인
- 빈 데이터 처리 확인
- 더미 데이터가 데이터 검증을 통과하는지 확인
- 데이터 소스를 교체해도 UI 코드가 직접 변경되지 않는 구조인지 확인
- Figure 생성 함수가 Plotly Figure를 반환하는지 확인
- AgGrid `rowData`와 `columnDefs`가 분리되어 있는지 확인
- 정적 HTML 생성 확인
- 생성된 HTML을 오프라인에서 열 수 있는지 확인
- 생성된 HTML의 외부 URL 참조 확인
- 외부 CSS, JavaScript, 폰트, 이미지 요청 확인
- 변경한 파일과 실행한 명령 기록

검증을 실행하지 못한 경우에는 실행하지 못한 이유를 결과 보고에 작성한다.
실행하지 않은 검증을 성공했다고 보고하면 안 된다.

---

## 17. 금지사항

- CDN 사용 금지
- Google Fonts 사용 금지
- 외부 폰트 자동 다운로드 금지
- 외부 CSS 자동 로딩 금지
- 외부 JavaScript 자동 로딩 금지
- 실행 시 원격 이미지 로딩 금지
- 실제 개인정보를 더미 데이터로 사용 금지
- 비밀번호나 인증 정보를 코드에 저장 금지
- UI 코드에서 CSV 직접 읽기 금지
- UI 코드에서 데이터베이스 직접 접속 금지
- 콜백 안에 SQL 직접 작성 금지
- 승인 없는 패키지 추가 금지
- 승인 없는 외부 JavaScript 추가 금지
- 임의의 업무 지표 정의 금지
- 임의의 데이터 컬럼 정의 금지
- 이 문서에 구체적인 화면 콘텐츠 작성 금지
- Dash 앱 전체가 제한 없이 정적 HTML로 변환된다고 설명하는 것 금지
- 테스트하지 않은 작업을 테스트 완료라고 보고하는 것 금지
- 사용자가 요청하지 않은 대규모 리팩터링 금지
- 기존 파일을 확인하지 않고 전면 덮어쓰기 금지

---

## 18. 미결정 사항

아래 항목은 확정되지 않았으며 임의로 결정하지 않는다.

- 실제 데이터 소스
- 실제 파일 형식
- 실제 데이터베이스 종류
- 실제 API 또는 내부 시스템 연결 방식
- 실제 데이터 컬럼
- 업무 지표
- 계산식
- 필터 항목
- 차트 종류
- 테이블 컬럼
- 데이터 갱신 주기
- 인증 방식
- 운영 위치가 개인 PC인지 내부 서버인지
- 정적 HTML의 구체적인 화면 구성
- 정적 HTML의 테이블 상호작용 필요 여부
- `requirements.txt`에 고정할 정확한 패키지 버전
- 모든 실행 PC에 Spoqa Han Sans가 설치되어 있는지
