# AGENTS.md

Claude Code와 OpenAI Codex가 공통으로 따르는 프로젝트 규칙의 단일 원본이다.
도구별 작업 절차는 이 문서에 넣지 않는다. 규칙은 한 곳에만 적고, 필요하면 섹션을 참조한다.

## 1. 프로젝트 목적

Python 기반 업무용 데이터 대시보드. 산출물은 두 가지다 — Dash 서버로 실행하는 대화형
대시보드, 그리고 서버 없이 브라우저에서 여는 정적 HTML 보고서(스냅샷).
데이터는 사내에서 만든 파일을 읽는다.
실행 환경은 인터넷이 차단된 내부망을 전제로 한다.

## 2. 공통 기술 스택

Python 3.13, Dash, Plotly, dash-ag-grid, pandas, HTML/CSS, Python 표준 라이브러리.
Node.js와 React를 직접 사용하지 않는다.

사용자 승인 없이 추가하지 않는 것: dash-bootstrap-components, Bootstrap, Tailwind CSS,
Font Awesome, 외부 JavaScript 테이블 라이브러리, 외부 CSS 프레임워크, Google Fonts,
기타 CDN 기반 라이브러리.

새로운 Python 패키지가 필요하면 설치하거나 `requirements.txt`에 추가하기 전에
① 필요한 이유 ② 기존 스택으로 어려운 이유 ③ 내부망 사용 가능 여부
④ 정적 HTML 내보내기 영향 ⑤ 추가하지 않는 대안을 설명하고 승인을 받는다.

## 3. 최우선 제약사항

우선순위가 충돌하면 아래가 다른 편의성보다 앞선다.

1. 내부망·오프라인 실행 가능성 → §7
2. 데이터 계층과 UI 계층의 분리 → §9, §10
3. Dash 화면과 정적 HTML의 공통 로직 재사용 → §14
4. 디자인 토큰 일원화 → §5
5. 최소 범위 수정 (요청 없는 리팩터링 금지)

## 4. 문서의 범위

이 문서는 디자인·개발 규칙만 정의한다. 업무 내용은 확정되지 않았으므로 다음을 적지 않는다.

- KPI·지표 이름, 탭 이름, 화면 문구, 대시보드 제목
- 차트 제목·개수·종류·축 구성, 지표 계산식, 전월/전년 대비 기준
- 데이터·테이블 컬럼명, 필터 항목, 구체적인 업무 영역
- 실제 데이터베이스 종류·테이블명·API 주소·사내 시스템 이름

### 4.1 레이아웃 골격

손그림 스케치는 저장소에 포함하지 않는다. 스케치에서 확인한 골격만 아래에 남기며,
스케치의 업무 내용은 옮기지 않는다. 위에서 아래로 다섯 개 영역을 쌓는다.

1. 제목 영역.
2. 요약 카드 행 — 균등 폭 카드를 가로로 나열. 카드마다 큰 수치 하나와 아래 보조 비교값
   한 줄, 카드 높이는 동일하게 맞춘다.
3. 가로 탭 형태의 탐색 영역 — 단일 선택, 선택된 탭을 강조.
4. 차트 패널 다열 그리드 — 패널 좌상단에 제목, 우상단에 해당 패널의 선택 컨트롤,
   범례와 축 레이블은 패널 내부.
5. 화면 폭 전체를 쓰는 상세 테이블 — 컬럼 헤더에 정렬·필터 컨트롤, 컬럼이 많으면
   가로 스크롤 또는 컬럼 표시 옵션을 고려.

전역 선택 영역과 패널별 선택 컨트롤이 공존할 수 있는 구조로 설계한다.
요약 → 탐색 → 분석 → 상세의 시각적 계층을 유지하고, 밀도가 높아 세로 스크롤을 허용한다.

## 5. 디자인 시스템

참고 자료는 미래에셋증권 CI 가이드
(https://securities.miraeasset.com/newir/view/pc/kr/ciguide.jsp)이며 디자인 확인용일 뿐이다.
애플리케이션이 실행 중 이 URL에 접근하거나 여기서 CSS·폰트·이미지·JS를 불러오면 안 된다.

### 5.1 색상 토큰

- 주색상 — Orange `#F58220`, Blue `#043B72`
- 그래프·시각화 보조색 — `#F0B26B`, `#CB6015`, `#AD624E`, `#84888B`,
  `#00A9CE`, `#0086B8`, `#7E9FC3`, `#8DC8E8`
- 배경·구분선 보조색 — `#48535B`, `#C2AC97`, `#84888B`, `#A0A6A8`,
  `#CDCECB`, `#E5E4E1`, `#ECEFF4`

### 5.2 사용 원칙

- Orange를 핵심 강조색, Blue를 보조색으로 쓰되 Orange를 화면 전체에 과도하게 쓰지 않는다.
- 일반 배경은 흰색 또는 밝은 회색, 카드와 주요 콘텐츠 배경은 흰색, 구분선은 밝은 회색,
  본문 텍스트는 검정에 가까운 회색을 사용한다.
- 상태나 의미를 색상만으로 구분하지 않는다(레이블·아이콘·텍스트 병행).

### 5.3 토큰 관리

색상·글꼴 값을 CSS나 Plotly 코드에 직접 반복해 적지 않는다.
CSS 변수와 Python 상수로 각각 한 곳에서만 관리하고, 양쪽 이름의 의미를 일치시킨다.
Plotly 차트도 CSS와 동일한 토큰을 사용한다.

### 5.4 글꼴

기본 글꼴 스택: `"Spoqa Han Sans Neo", "Spoqa Han Sans", "Malgun Gothic", sans-serif`

- 실행 PC에 설치된 Spoqa Han Sans를 우선 쓰고, 없으면 Malgun Gothic 또는 sans-serif로 대체한다.
- 외부 URL이나 Google Fonts에서 폰트를 불러오지 않는다. 사용자가 제공하지 않은 폰트 파일을
  자동으로 다운로드하지 않고, 라이선스가 확인되지 않은 폰트 파일을 저장소에 추가하지 않는다.
- Plotly `layout.font.family`에도 동일한 스택을 쓰고, 공통 설정에서 한 번만 관리한다.

## 6. UI 및 레이아웃 원칙

- 데스크톱 내부 업무 환경을 우선하되, 화면이 좁아지면 다열 구성이 한 열로 접히게 한다.
- 레이아웃은 CSS Grid와 Flexbox로 구성하고 고정 픽셀 너비를 과도하게 쓰지 않는다.
- 주요 영역의 좌우 정렬과 간격을 일관되게 유지하고, 카드·차트·필터·테이블의
  시각적 계층을 명확히 한다.
- 과도한 그림자·그라데이션·장식 효과를 쓰지 않고, 모서리 둥글기는 작게 절제한다.
- 숫자 표시 형식은 공통 함수로 관리한다.
- 키보드 포커스 표시를 제거하지 않는다.
- 컴포넌트 ID는 의미 있는 이름을 쓰고 프로젝트 전체에서 규칙을 통일한다.
- 구체적인 화면 콘텐츠는 코드 구현 시 별도 요구사항으로 정의한다.

## 7. 내부망 및 오프라인 원칙

실행에 외부 인터넷 연결을 요구하지 않는다. 이는 다른 편의성보다 우선한다.

- CDN, 외부 URL의 CSS·JavaScript·폰트·이미지·아이콘을 사용하지 않는다.
- 원격 API를 기본 데이터 소스로 쓰지 않고, 실행 중 CI 가이드 사이트에 접근하지 않는다.
- CSS와 이미지는 프로젝트 내부 `assets`에서 관리하고, Dash가 프론트엔드 파일을
  로컬에서 제공하도록 구성한다.
- dash-ag-grid는 설치된 Python 패키지의 로컬 리소스를 사용하며,
  외부 JavaScript 파일을 직접 삽입하지 않는다.
- 실행 시 패키지를 자동으로 다운로드하는 코드를 작성하지 않는다.
- 실제 접속·인증 정보를 저장소에 넣지 않는다(→ §10.2).

패키지 반입: 버전은 `requirements.txt`에서 고정할 수 있게 하고, 의존성 목록을 명확히
관리한다. 필요하면 인터넷 환경에서 wheel을 미리 준비해 내부망으로 반입한다.

오프라인 검증 절차는 §16에 있다.

## 8. 권장 프로젝트 구조

처음부터 과도하게 복잡하게 만들지 않는다.

```
project-root/
├── app.py
├── export_html.py
├── dashboard/
│   ├── __init__.py
│   ├── data.py
│   ├── format.py
│   ├── metrics.py
│   ├── figures.py
│   ├── grid.py
│   ├── layout.py
│   ├── callbacks.py
│   ├── sources/          원본 파일마다 한 모듈
│   │   ├── __init__.py
│   │   ├── monthly.py
│   │   └── customer2.py
│   └── tabs/             탭마다 한 폴더
│       ├── __init__.py
│       ├── registry.py
│       └── customer/
│           ├── __init__.py
│           ├── metrics.py
│           └── figures.py
├── assets/
│   └── style.css
├── data/
├── tests/
├── requirements.txt
├── AGENTS.md
└── CLAUDE.md
```

역할 (`dashboard/` 하위 모듈은 파일명만 표기)

- `app.py` — Dash 앱 생성과 실행 진입점. 레이아웃·데이터 로직을 여기에 쌓지 않는다.
- `export_html.py` — 정적 HTML 보고서 생성 진입점(→ §14). `app.py`와 같은
  계층에 두어 `python export_html.py`로 바로 실행한다. 두 산출물이 같은 값을
  보여주도록 `dashboard` 모듈을 그대로 쓴다.
- `data.py` — 데이터 소스 선택, 파일 찾기, 정규화·검증, `DashboardData` 계약.
  데이터 접근의 유일한 경로다(→ §9).
- `sources/` — 원본 파일마다 한 모듈. 그 파일의 컬럼표·조립·대조만 담는다.
  원본이 늘어도 `data.py`는 커지지 않는다(→ §9).
- `format.py` — 숫자·날짜 표기. Dash 화면과 정적 HTML이 함께 쓴다.
- `metrics.py` — 어느 탭에서나 쓰는 계산 도구와 KPI 값.
- `figures.py` — 디자인 토큰과 Plotly 공통 레이아웃·축 설정(→ §12).
- `grid.py` — `Column` 선언을 받아 `columnDefs`·`rowData`를 만드는 일반 함수(→ §13).
  정적 HTML 테이블에서도 쓰는 공통 포맷을 여기서 관리한다. 특정 표의 컬럼
  목록은 여기 두지 않는다.
- `tabs/` — 탭마다 한 폴더. 그 탭의 계산·Figure·카드 선언·표 컬럼을 담는다.
  `registry.py`가 선언 자료형을, `__init__.py`가 등록표와 탭 순서를 갖는다(→ §8.1).
- `layout.py` — 페이지 골격과 재사용 UI 컴포넌트. 데이터를 직접 읽지 않고,
  어떤 탭에 어떤 카드가 있는지도 적지 않는다. 등록표를 돌면서 선언대로 그린다.
- `callbacks.py` — 첫 화면 값 계산과 콜백 등록(→ §11). 등록표를 돌면서
  선택 컨트롤이 있는 차트마다 하나씩 등록한다.
- `assets/style.css` — 공통 CSS, 디자인 토큰, 반응형 레이아웃.
  정적 HTML에서만 쓰는 스타일은 여기 두지 않는다(→ §14).
- `data/` — 실행 시 읽을 실제 데이터 파일을 두는 곳. 저장소에 넣지 않는다.
- `tests/` — 데이터 계약·정규화, Figure 생성, 그리드 설정, HTML 내보내기 검증.

필요해지기 전에는 `services`, `repositories`, `helpers`, `utils`, `components`,
`adapters`, `config`, `docs` 디렉터리를 만들지 않는다. 역할 분리가 실제로 필요할 때만 확장한다.

### 8.1 탭 등록표

탭이 늘어날 것을 전제로 한다. 탭 하나가 무엇을 보여주는지는 그 탭 모듈에서
`Tab`·`Chart`·`Table` 선언으로 **한 번만** 적는다. 제목·선택 목록·안내 문구·확대
허용 여부를 두 곳에 적지 않는다.

`layout.py`·`callbacks.py`·`export_html.py`는 무엇을 그릴지 스스로 정하지 않고
등록표를 돌면서 선언대로 그린다. Dash 화면과 정적 HTML이 같은 선언을 읽으므로
두 산출물이 갈라지지 않는다(→ §14).

컴포넌트 ID는 손으로 적지 않고 탭 값과 차트 키에서 만든다. 탭이 늘어도 ID가
겹치지 않는다(→ §6, §11).

**탭을 추가할 때** — `tabs/` 아래에 폴더를 하나 만들고 `TABS`에 한 줄 더한다.
아직 만들지 않은 탭은 `TAB_ORDER`에만 두면 이름이 비활성으로 나타난다.
다른 파일은 고치지 않는다. 고쳐야 한다면 선언이 한 곳에 모여 있지 않다는 뜻이다.

## 9. 데이터 소스 분리

- 레이아웃·차트·그리드·콜백은 CSV를 직접 읽거나 데이터베이스에 직접 접속하지 않는다.
  데이터 접근은 `dashboard/data.py`를 통해서만 한다.
- 외부에는 일관된 로딩 함수만 공개한다. 예: `load_dashboard_data(filters=None)`
  (형태 예시이며 반환 컬럼은 정의하지 않는다.)
- 소스가 달라도 같은 반환 구조를 갖게 하고, 소스별 차이는 데이터 계층에서 흡수한다.
- 연결 방식이 바뀌어도 UI 코드를 수정하지 않아도 되게 설계한다.

데이터 소스 선택: `local_file`과 `internal_source`를 교체할 수 있게 하고, 선택값을
여러 곳에 하드코딩하지 않는다. 환경 변수 또는 하나의 공통 설정값으로 정한다
(예: `DASHBOARD_DATA_SOURCE`). 실제 데이터베이스 종류나 연결 방식은 확정하지 않는다.

읽을 파일 이름은 그 원본 모듈(`dashboard/sources/<이름>.py`) 한 곳에 적고,
환경 변수로 덮어쓸 수 있게 한다. 파일 이름만 적으면 `data/`에서 찾고, 현재 작업
폴더를 기준으로 찾지 않는다. 어디서 실행하든 같은 파일을 가리켜야 한다.

**원본 모듈 분리** — 원본 파일마다 컬럼 이름과 형태가 다르다. 그 차이를 흡수하는
코드는 `dashboard/sources/` 아래 그 파일의 모듈에만 둔다. 컬럼표, 표준 형태로
펴는 함수, 그 파일 안에서 숫자가 맞는지 보는 대조가 여기 해당한다.
여러 원본을 서로 맞춰 보는 대조는 원본 하나에 속하지 않으므로 `sources/__init__.py`가
맡는다. `data.py`에는 파일 찾기·정규화·검증 엔진과 `DashboardData` 계약만 남긴다.

**원본을 추가할 때** — `sources/`에 모듈을 하나 만들고 `SOURCES`에 한 줄 더한 뒤,
`assemble`에서 만든 프레임을 `DashboardData`에 넣는다. 표준 프레임이 늘면
`FRAME_NAMES`와 `DashboardData`에 필드를 더한다. UI 계층은 고치지 않는다.

`local_file`은 pkl 파일을 읽으며 두 가지 형태를 받는다. `DASHBOARD_DATA_FILE`
하나만 지정하면 표준 이름 4개를 키로 갖는 dict를 담은 파일로 읽고,
`DASHBOARD_CUSTOMER2_FILE`까지 지정하면 원본 파일 두 개를 데이터 계층이 표준 4개
형태로 조립한다. 원본 컬럼명이 표준 이름과 다르면 데이터 계층의 매핑표에만 적는다.
pickle은 파일을 여는 것만으로 그 안의 코드가 실행될 수 있으므로 사내에서 직접
만든 파일만 사용한다.

원본이 여러 파일로 나뉘어 시점이 다를 수 있으면 데이터 계층이 서로 대조한다.
같은 지표가 두 파일에 겹쳐 있으면 값이 같은지 확인하고, 다르면 어느 지점이
얼마나 다른지 알리며 멈춘다.

원본이 비율을 이미 계산해 담고 있으면 그 값을 그대로 화면까지 전달한다.
인원수로 되돌렸다가 비율을 다시 만들면 반올림 때문에 화면 숫자가 원본과 달라진다.
원본이 인원수만 주면 그때 데이터 계층이 비율을 계산한다.

화면이 쓰는 지표가 원본에 없으면 비워 둔 채로 넘기고 화면에 `-`로 표시한다.
없는 값을 0으로 채우지 않는다. 0은 "없음"이 아니라 "0으로 측정됨"을 뜻한다.

원본이 지점명을 빼고 전부 숫자여도 데이터 계층이 흡수한다 — 기준 월은 `202601`과
`2026-01`을 모두 읽고, 지점 코드는 문자열로 맞추며, 분류값이 숫자 코드면 코드표로
이름을 붙인다. 동의·불원이 각각 인원수 컬럼으로 오면 표준 형태로 편다.

원본에 '전체' 합계 행이 들어 있으면 지점 데이터에서 분리해 따로 들고 간다. 그대로
두면 합계가 지점 하나로 섞여 모든 숫자가 두 배가 된다. 화면의 전체 값은 그 '전체'
행을 그대로 쓰고, 더할 수 있는 값(인원수)은 지점 합계와 대조해 다르면 멈춘다.
비율은 더할 수 없으므로 대조하지 않고 원본 값을 그대로 믿는다.

정규화는 값을 조용히 고치지 않는다. 읽을 수 없는 숫자를 0으로 덮거나 모르는 분류값을
결측으로 바꾸지 않고, 어느 컬럼의 어떤 값이 문제인지 알리며 멈춘다. 틀린 숫자가 맞는
것처럼 화면에 뜨는 쪽이 더 위험하다.

검증은 데이터가 스스로 앞뒤가 맞는지만 확인한다. 기간이 몇 개월인지, 지점이 몇 개인지는
실제 데이터에서 달라질 수 있으므로 요구하지 않는다. 그 두 가지는 테스트가 표본 파일을
점검할 때만 확인한다.

데이터 정규화는 데이터 계층이 담당한다 — 컬럼명 매핑, 타입 변환, 누락값 처리,
필수 컬럼 확인, 날짜·숫자 형식 정규화, 중복·빈 데이터 처리, 유효성 검사.
원본 형태가 달라도 UI 계층에는 동일한 형태로 전달한다.

## 10. 표본 데이터와 실제 데이터 연결

### 10.1 표본 데이터

애플리케이션은 실제 데이터 파일만 읽는다. 표본 데이터를 만들어 내는 코드나
표본 전용 실행 경로를 애플리케이션에 두지 않는다.

- 테스트가 쓸 표본은 `tests/data/`에 두고, 실제 원본과 같은 형식으로 저장해
  같은 어댑터로 읽는다. 표본 전용 경로를 따로 두면 실제 데이터가 지나는 길이
  한 번도 검증되지 않는다.
- 실제 개인정보처럼 보이는 값을 쓰지 않는다. 실명·전화번호·계좌번호·주소를
  넣지 않고, 필요하면 일반적인 가상 식별자만 쓴다.
- 실제 원본에 없는 지표는 표본에도 넣지 않는다. 표본에만 있으면 그 지표가
  실제로는 비어 있다는 사실이 개발 중에 드러나지 않는다.
- 기간·지점 수처럼 실제 데이터에서 달라지는 값은 애플리케이션 코드에 적지 않고
  테스트 쪽에만 적는다.

### 10.2 실제 데이터 연결

교체·추가 대상은 데이터 읽기 구현, 원본↔내부 표준 컬럼 매핑, 인증·연결 설정, 조회 조건뿐이다.
레이아웃, Figure 생성 함수, 그리드 설정 함수, 공통 포맷 함수, HTML 내보내기 로직은
최대한 변경하지 않도록 설계한다.

접속 정보(서버 주소, 포트, 사용자 ID, 비밀번호, 인증 토큰, 데이터베이스명, 내부 API 주소)는
코드에 적지 않고 환경 변수나 내부 보안 설정으로 전달한다.
실제 접속·인증 방식은 임의로 결정하지 않는다.

## 11. Dash 및 콜백 규칙

- 앱 생성·레이아웃·콜백·데이터 로직을 분리한다.
- 콜백에서 파일을 직접 읽거나 SQL을 작성하거나 데이터 소스 종류를 판별하지 않는다.
- 하나의 콜백에 역할을 몰아넣지 않고 입출력 책임을 명확히 한다.
- 전역 가변 데이터를 콜백에서 수정하지 않고, 같은 전처리를 여러 콜백에서 반복하지 않는다.
- 초기 로딩이 무거워지지 않게 하고, 빈 데이터가 와도 화면이 깨지지 않게 한다.
- 오류를 무조건 숨기지 않고 필요하면 사용자가 이해할 수 있는 오류 상태를 표시한다.
  운영 코드에 `debug=True`를 고정하지 않는다.
- 존재하지 않는 ID를 참조하지 않으며, 요청 없이 `suppress_callback_exceptions`를 남용하지 않는다.

## 12. Plotly 규칙

- Figure 생성 함수는 데이터를 입력받아 Figure를 반환하고, 데이터를 직접 조회하지 않는다.
- 공통 레이아웃 설정을 재사용하고 색상·글꼴은 §5의 토큰에서 가져온다.
- hover template 형식을 일관되게 관리하고, 불필요한 배경·격자선·modebar 기능을 정리한다.
- 반응형 크기를 고려하고, 데이터가 비면 빈 Figure 또는 안내 상태를 반환할 수 있게 한다.
- 정적 HTML도 같은 Figure 함수를 쓴다. 화면용과 HTML용을 중복 구현하지 않는다.
- 구체적인 차트 종류·제목·축은 정의하지 않는다.

## 13. dash-ag-grid 규칙

- Dash 화면의 업무용 테이블에는 `dash_ag_grid.AgGrid`를 쓰고, 공식 의존성으로 관리한다.
  Python 3.13와 호환되는 검증된 버전을 `requirements.txt`에 고정한다.
- **dash-ag-grid는 Dash 화면 전용이다. 정적 HTML 내보내기가 이 패키지를 요구하면
  안 된다.** 내부망에는 이 패키지가 없을 수 있고, 그래도 HTML은 만들어져야 한다.
  그래서 `import dash_ag_grid`는 실제로 화면의 표를 그리는 함수 안에 둔다. 파일
  맨 위에 두면 그 모듈을 불러오는 것만으로 설치를 요구하게 된다
  (→ `dashboard/layout.py`의 `_table_card`, `tests/test_export_html.py`).
- 외부 CDN이나 원격 JavaScript를 직접 참조하지 않는다.
- `rowData`와 `columnDefs` 생성을 각각 별도 함수로 분리하고, 레이아웃 파일에 복잡한
  `columnDefs`를 직접 쓰지 않는다. 같은 가공 로직을 콜백이나 레이아웃에 중복 작성하지 않는다.
- 정렬·필터·페이지네이션·컬럼 크기 조절을 지원할 수 있게 하되, 데이터 규모를 보고
  필요한 기능만 켠다. 숫자·날짜·비율 포맷은 공통 함수로 관리한다.
- 승인 없이 외부 JavaScript cell renderer를 추가하지 않는다.
  `unsafe_allow_jscode`는 꼭 필요한 경우에만 검토하며, 임의 JavaScript 실행을 피한다.

## 14. 정적 HTML 내보내기

Dash 실행 모드와 정적 HTML 모드는 서로 다른 산출물이다. 별도로 관리하되,
데이터 처리 함수와 Figure 생성 함수는 공유한다.

- **Dash 실행 모드** — Python 콜백, 서버 측 데이터 처리, 동적 갱신, AgGrid 기능,
  선택에 따른 재조회를 쓸 수 있다.
- **정적 HTML 모드** — 서버 없이 여는 특정 시점의 스냅샷이다.
  Dash 앱 전체가 그대로 HTML로 변환된다고 표현하지 않는다.

**생성 규칙** — Plotly 차트는 `to_html()` 또는 동등한 기능으로 변환한다. Plotly.js는
CDN이 아니라 HTML 안에 포함하되 한 파일에 여러 번 들어가지 않게 한다. CSS는 HTML 내부에
포함하거나 로컬 파일을 쓰고, 외부 JS·CSS·폰트를 참조하지 않는다. 결과물은 인터넷 없이 열려야 한다.

**디자인 일치** — 정적 HTML은 `assets/style.css`를 그대로 넣고 화면과 같은 클래스 이름을
쓴다. 여백·글꼴·색을 내보내기 쪽에 다시 적으면 화면 디자인을 고쳤을 때 두 산출물이 갈라진다.
Dash 컴포넌트(`dcc.Tabs`·`dcc.Dropdown`·`AgGrid`)를 대신 그리는 곳에만 내보내기 전용 CSS를
두고, 거기서도 화면이 쓰는 값(헤더·행 높이, 컬럼 너비, 목록 높이)은 숫자로 적지 않고
`dashboard`의 상수에서 채워 넣는다.

**AgGrid 변환** — AgGrid 컴포넌트 자체를 정적 HTML에 저장하려 하지 않는다. 동일한 원본
데이터와 동일한 정규화·포맷 함수를 써서 일반 HTML `<table>`로 렌더링한다(pandas
`DataFrame.to_html()` 또는 별도의 안전한 테이블 생성 함수). 숫자 정렬·헤더 스타일·행
구분선은 CSS로 구현하고, HTML 특수문자를 이스케이프해 데이터에 포함된 HTML이 실행되지 않게 한다.

**기능 차이** — Python 콜백과 AgGrid는 정적 HTML에 담기지 않는다. 화면에서 같은 동작이
필요하면 브라우저 안에서 도는 코드로 다시 만든다.

- 그대로 유지됨 — hover, 확대·축소, 범례 선택 등 Plotly 기본 상호작용
- 다시 만든 것 — 지점 선택(고를 수 있는 값마다 Figure를 미리 담아 두고
  `Plotly.react`로 교체), 선택 목록(브라우저 기본 `select`는 펼친 목록을
  운영체제가 그려 CSS가 닿지 않으므로 목록까지 직접 그림), 표 정렬(셀에 서식 없는
  원본 값을 함께 넣고 헤더 클릭으로 다시 줄 세움), 컬럼 너비(화면의 flex와 같은
  규칙으로 남는 폭을 나눔)
- 담기지 않음 — 서버 측 재조회, 기간·필터 변경처럼 데이터를 다시 읽어야 하는 동작.
  그런 변경은 파일을 새로 만든다.

다시 만들 때는 문서 안에 직접 쓴 코드만 쓴다. 승인 없이 외부 JavaScript·테이블
라이브러리를 추가하지 않는다. 고를 수 있는 값마다 Figure를 담으므로 파일이 커진다.
지점 수가 늘면 크기를 확인한다.

## 15. 코딩 규칙

- Python 3.13 기준. 변수·함수는 snake_case, 클래스는 PascalCase, 상수는 UPPER_SNAKE_CASE.
- 사용자에게 보이는 문구는 한글을 기본으로 하고, 타입 힌트를 가능한 범위에서 쓴다.
- 함수는 한 가지 역할만 갖게 하고 데이터 처리와 UI 생성을 분리한다.
- 파일 경로, 데이터 소스 설정, 디자인 값, 숫자·날짜 포맷을 여러 곳에 중복하지 않는다.
- 빈 데이터와 누락값을 처리하고, 예외를 무조건 무시하지 않는다.
- 실제 개인정보를 로그에 출력하지 않는다.
- 불필요한 추상화나 클래스 계층을 만들지 않고 현재 규모에 맞게만 분리한다.
- 요청받지 않은 대규모 리팩터링을 하지 않고, 기존 동작을 유지하며 최소 범위로 수정한다.

### 15.1 줄 길이 79칸 (필수)

모든 소스 파일(`.py`, `.css`)은 **한 줄이 79표시칸을 넘지 않는다.** 한글·전각 문자는
2칸으로 센다. 새 코드를 쓸 때도, 기존 코드를 고칠 때도 지킨다.

이건 스타일 취향이 아니라 내부망 반입 조건이다. 코드를 폐쇄망으로 옮기는 클립보드
경로가 80칸에서 줄을 잘라 넣는다. 잘린 자리가 코드 한가운데면 `IndentationError:
unexpected indent`, 문자열 한가운데면 `SyntaxError: unterminated string literal`이
난다. 원인이 드러나지 않아 찾는 데 오래 걸린다.

줄을 나누는 방법 — 코드는 괄호 안에서 나누고, 문자열은 인접 리터럴로 쪼개고
(`"앞부분" "뒷부분"`), 주석·docstring은 문장 단위로 접는다. 값과 동작은 그대로 둔다.
자동 정리는 `black -l 79`로 하되, 한글이 든 줄은 문자 수와 표시칸이 달라 걸러지지
않으므로 직접 확인한다.

파일 끝에 이전 내용이 남지 않게 하고, 줄바꿈은 LF로 통일한다. CRLF는 옮기는 과정에서
줄바꿈 두 개로 읽혀 파일이 두 배로 늘어난다.

검사 방법은 §16의 오프라인 항목에 있다.

## 16. 테스트 및 검증

코드 수정 후 가능한 범위에서 수행한다.

- 기본 — Python 버전 확인, 3.13 환경에서 import 확인, `python -m compileall .`, 관련 테스트 실행
- Dash — 앱 시작과 초기 렌더링 확인, 콜백 ID 중복 및 존재하지 않는 ID 참조 확인,
  빈 데이터 처리 확인
- 데이터 — 표본 파일이 검증을 통과하는지, 소스를 교체해도 UI 코드를 고치지 않는 구조인지 확인
- 구조 — Figure 함수가 Plotly Figure를 반환하는지, `rowData`와 `columnDefs`가 분리됐는지 확인
- 오프라인 — 네트워크를 차단한 상태로 Dash 앱 실행, 개발자 도구에서 외부 요청 확인,
  소스의 `http://`·`https://` 검색, CSS의 `@import`와 외부 `url()` 확인,
  로컬 `assets` 누락 확인
- 정적 HTML — 생성 확인, 네트워크 없이 열어 확인, 외부 스크립트·스타일·폰트·이미지 참조 확인
- 반입 — 줄 길이 79칸 이하 확인(→ §15.1). 아래 명령이 `over79 = 0`을 내야 한다.

```
python -c "import pathlib, unicodedata; w=lambda s: sum(2 if unicodedata.east_asian_width(c) in 'WF' else 1 for c in s); print('over79 =', sum(1 for p in [*pathlib.Path('.').glob('*.py'), *pathlib.Path('dashboard').rglob('*.py'), *pathlib.Path('assets').glob('*.css')] for l in p.read_text(encoding='utf-8').split(chr(10)) if w(l) > 79))"
```

변경한 파일과 실행한 명령을 기록한다. 실행하지 못한 검증은 그 이유를 보고에 적고,
실행하지 않은 검증을 성공했다고 보고하지 않는다.

## 17. 미결정 사항

아래는 확정되지 않았으며 임의로 결정하지 않는다.

- 데이터 — 실제 소스, 데이터베이스 종류, API·내부 시스템 연결 방식,
  원본 컬럼명, 갱신 주기, 인증 방식
- 업무 — 지표, 계산식, 필터 항목, 차트 종류, 테이블 컬럼
- 배포·환경 — 운영 위치(개인 PC 또는 내부 서버),
  모든 실행 PC에 Spoqa Han Sans 설치 여부
- 정적 HTML — 구체적인 화면 구성, 테이블 상호작용 필요 여부
